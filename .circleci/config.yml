version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
  build:
    docker:
      - image: maven:3.8.2-eclipse-temurin-11
    steps:
      - run:
          name: "Build"
          command: "mvn clean package -DskipTests"
      - run:
          name: "Tests"
          command: "mvn test"


workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - build